async def get_current_user_clerk_id() -> str:
    # In Sprint 1, this would decode the Authorization Header using clerk SDK
    # For now, it returns a mock to allow the walking skeleton to function
    return "mock_clerk_id"
